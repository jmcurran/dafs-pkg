StangleBook = function(idx = 0:6, path = ""){

    for(i in idx){
        path = paste(path,"Chapter",i,sep="")
        setwd(path)
        fname = paste("ch",i,".rnw", sep = "")
        Stangle(fname)
        setwd("..")
    }
}
